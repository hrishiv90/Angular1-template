"use strict";

module.exports = function($scope) {
    $scope.pageTitle = 'Home';

    // Loader for ajax calls
    // $scope.toggleLoader();

    // Sample Success Msg Box
    // $scope.toggleMessage('success', 'This is success msg box');

    // Sample Error Msg Box
    // $scope.toggleMessage('error', 'This is error msg box');

    // Sample Confirm Box
    // $scope.toggleMessage('confirm', { title: 'Confirm', body: 'This is custom confirm box', okBtnText: 'Yes' });
};